<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!--
      Launch theme. Parent is androidx core-splashscreen, whose API is the
      `windowSplashScreen*` attributes — the template only set
      `android:background="@drawable/splash"`, which is the pre-core-splashscreen
      way and left the stock 480x320 Capacitor placeholder image showing on a
      pale ground. Using a solid brand colour makes launch one continuous colour
      into the app instead of a pale flash that swaps to dark.

      `postSplashScreenTheme` is what the activity becomes after the splash;
      without it the launch theme stayed applied. AppTheme.NoActionBar was
      declared by the template and used by nothing until now.

      `windowSplashScreenAnimatedIcon` puts the EventFlow mark on the launch
      screen. It is the MARK ONLY, not the full lockup: the system masks this
      drawable to a circle and scales it, so a wordmark placed here is cropped
      away on the very first frame the user sees. The full lockup lives in
      @drawable/splash, which is what the Capacitor plugin draws on the older
      code path. Both come out of scripts/brand-assets.mjs.
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <item name="android:background">@color/splashBackground</item>
    </style>
</resources>