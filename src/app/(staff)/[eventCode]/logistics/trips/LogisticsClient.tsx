/**
 * Re-export. The canonical trip-planning screen is `logistics/LogisticsClient`.
 * `logistics/trips` is the same screen under a second path, kept so existing
 * links do not 404.
 */
export { LogisticsClient } from '../LogisticsClient'
