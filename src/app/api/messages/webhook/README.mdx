/**
 * POST /api/messages/webhook
 *
 * M2 static export: API routes cannot be dynamically served from a static
 * bundle. The WhatsApp BSP callback must be handled by a Supabase Edge
 * Function — deploy handleWebhook as an Edge Function at:
 *
 *   supabase/functions/message-webhook/index.ts
 *
 * and point the BSP's webhook URL at:
 *
 *   https://xktxnkuzplhzxkevwrcj.supabase.co/functions/v1/message-webhook
 *
 * The Edge Function body is the same as what the old route.ts ran:
 * parse body → handleWebhook() → return status.
 */
