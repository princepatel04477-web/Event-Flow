---
tags: [session-log, auto-captured]
session: 628dcdf1-39c7-4239-89d4-4130030dfcb5
captured_by: SessionEnd
started: 2026-09-21 23:06
updated: 2026-09-21 23:09
---

# Session 2026-09-21 2306 (628dcdf1)

Auto-captured from the Claude Code transcript on `SessionEnd`.
Curated project knowledge lives in [[EventFlow]]. This note is raw history.

## What was asked

- Review the uncommitted change to src/app/(staff)/[eventCode]/rsvp/status/[groupId]/RsvpLogForm.tsx (run `git --no-pager diff -- that file`). READ-ONLY: do not edit anything. It removes an `await releaseGroupAfterCall(...)` and fires it in the background instead, so the screen has one awaited round trip instead of two. The claim being made is: `save_rsvp_log` nulls `locked_until`, every lock reader ...

## Commands run (17 total, 17 distinct)

```
git --no-pager diff -- "src/app/(staff)/[eventCode]/rsvp/status/[groupId]/RsvpLogForm.tsx"
ls supabase/migrations | head -80 && echo "---" && ls supabase/migrations | grep -E "20260807000502|20260808100000"
grep -rn "save_rsvp_log" supabase/migrations/ | head -40
sed -n 125,225p supabase/migrations/20260807000502_code_auth_attribution.sql
grep -n "locked_by\|locked_until" supabase/migrations/20260807000502_code_auth_attribution.sql
grep -n "locked_by_staff\|create or replace function\|locked_until" supabase/migrations/20260808100000_attribution_split.sql
sed -n 355,415p supabase/migrations/20260808100000_attribution_split.sql
cat supabase/migrations/20260813000000_release_group_no_admin_override.sql
sed -n 1,60p "src/app/(staff)/[eventCode]/rsvp/status/[groupId]/RsvpLogForm.tsx"
sed -n 150,275p "src/app/(staff)/[eventCode]/rsvp/status/[groupId]/RsvpLogForm.tsx"
grep -rn "locked_by_staff\|locked_until\|locked_by" src/ --include=*.ts --include=*.tsx | grep -v "^src/app/(staff)/\[eventCode\]/rsvp/status/\[groupId\]/RsvpLo ...
sed -n 315,360p src/lib/actions/call.ts && echo "=== page.tsx 40-110" && sed -n 40,110p "src/app/(staff)/[eventCode]/rsvp/status/[groupId]/page.tsx"
grep -rn "is_locked" supabase/migrations/*.sql | head -20
grep -n "claimGroupForCall" -A 80 src/lib/actions/call.ts | sed -n 1,110p
find "src/app/(staff)/[eventCode]/rsvp/next" -type f | head && echo "===" && cat "src/app/(staff)/[eventCode]/rsvp/next/page.tsx" 2>/dev/null | head -80
sed -n 55,110p supabase/migrations/20260812000000_presence_columns.sql
grep -n "attempt_id\|call_attempts\|attemptId" src/lib/actions/rsvp.ts | head -20
```

## Where it left off

1. **Mostly TRUE, with one overstatement.**
   - `save_rsvp_log` nulls the lock: `supabase/migrations/20260807000502_code_auth_attribution.sql:194-195` (`locked_by = null, locked_until = null`) â€” line numbers in the comment are exact, and no later migration redefines the function (only `20260806100000` and `20260807000502` define it).
   - It does **not** touch `locked_by_staff`; that column is added later at `20260808100000_attribution_split.sql:81`. Confirmed â€” so the release is genuinely not redundant.
   - Blocking readers all gate on `locked_until`: `claim_group` WHERE clause `20260808100000:376-379`; `v_rsvp_queue.is_locked` = `(locked_until is not null and locked_until > now())` at `20260812000000_presence_columns.sql:78`; `releaseGroupAfterCall`'s `stillLocked` at `src/lib/actions/call.ts:345-347`; and the only path to the "locked" screen is `claim_group` raising `55P03` (`src/lib/actions/call.ts:91-103`).
   - **Overstatement:** "every reader â€¦ requires it to be non-null and in the future" is false as written. `src/app/(staff)/[eventCode]/rsvp/status/[groupId]/page.tsx:101` reads `group.locked_by_staff` with no `locked_until` check, to derive `callerId` for in-flight-attempt matching. Non-blocking, but it is a reader that a residual `locked_by_staff` feeds.

2. **Second caller wrongly blocked: no.** `locked_until` is null at commit of the same transaction, so `claim_group` and `is_locked` clear immediately; the background call cannot hold anyone out.
   **Lock released by a non-holder: yes â€” same-identity, different claim.** `release_group` (`20260813000000:66-69`) matches on identity only, not on claim generation, so an in-flight release fired for group G will wipe a *newer* lock on G held by the same caller. This is the exact hazard the migration header names ("An unawaited release racing a subsequent claim by the SAME caller still wipes that caller's own lock â€¦ Ordering at the call site is the only fix"), and the diff creates the unawaited call site it warns about.
   It is reachable, not theoretical: `saveRsvpLog` writes no `call_attempts` row, so a group logged without dialing stays `attempt_count = 0`, and `/rsvp/next` (`rsvp/next/page.tsx:31-40`, sorting `attempt_count` asc over an unfiltered `v_rsvp_queue`) can redirect straight back to the group just saved â€” whose `page.tsx` then re-claims it. Back-button reaches the same state. Cross-identity is safe (a different caller's claim doesn't match the releaser's identity).

3. Defects:
   - **Unhandled rejection.** `void releaseGroupAfterCall(...).then(cb)` has no `.catch()`. The `{ok:false}` branch is handled, but a rejected server action (Wi-Fi drop mid-flight â€” the documented failure mode) is not. Needs `.catch()`, not just `.then()`.
   - **Ordering.** See (2): the fix is to make the release lose to any later claim â€” sequence it before `router.push`, or drop it on navigation â€” not to re-await it.
   - **`holdsLock` guard.** Unchanged in substance, but backgrounding makes its staleness matter more: it is computed from the `group` prop rendered at page load, and the `release_group` it gates now resolves an arbitrary time later against whatever state exists then. The guard describes the past; the RPC acts on the present.
   - Adjacent, pre-existing, not in the diff: `src/lib/actions/call.ts:340-341` calls `revalidatePath` on `/${eventCode}/rsvp/queue` twice; the comment says it revalidates both queue routes, but `/${eventCode}/queue` is never revalidated.

---

See also: [[Status]], [[Roadmap]], [[Known Traps]], [[Schema Reality Check]]
