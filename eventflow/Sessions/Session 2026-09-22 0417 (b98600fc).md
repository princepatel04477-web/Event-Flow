---
tags: [session-log, auto-captured]
session: b98600fc-dde9-474f-a249-6019f61f2609
captured_by: SessionEnd
started: 2026-09-22 04:17
updated: 2026-09-22 04:29
---

# Session 2026-09-22 0417 (b98600fc)

Auto-captured from the Claude Code transcript on `SessionEnd`.
Curated project knowledge lives in [[EventFlow]]. This note is raw history.

## What was asked

- # R1 Ã¢â‚¬â€ adversarial review of the "Instant & Obvious" rebuild (V4 Ã¢â€ â€™ HEAD) You are a reviewer, not an implementer. **Change nothing under `src/`, `tests/`, `scripts/`, `e2e/` or `package.json`.** Do not commit, stage, stash, check out or clean. Your only output is a findings file and a summary. ## What you are reviewing The repository is `C:\dev\EventFlow` Ã¢â‚¬â€ a Next.js 16 / React ...

## Files created or modified

| File | Edits |
|---|---|
| `C:/dev/EventFlow-ui2/reviews/R1.md` | 1 |

## Commands run (73 total, 73 distinct)

```
cat /c/Users/rebel/AppData/Local/Temp/claude/C--dev-EventFlow/b98600fc-dde9-474f-a249-6019f61f2609/tasks/bovwp7o91.output 2>/dev/null | tail -20
npx vitest run 2>&1 | tail -30
cat src/lib/query/keys.ts
grep -rn "staleTime\|new QueryClient\|defaultOptions\|refetchOnWindowFocus\|refetchOnMount" src/components/ src/lib/query/ src/app/providers* src/app/layout.tsx ...
sed -n '25,90p' src/components/providers/QueryProvider.tsx
tail -15 /c/Users/rebel/AppData/Local/Temp/claude/C--dev-EventFlow/b98600fc-dde9-474f-a249-6019f61f2609/tasks/blmpmwp17.output
NEXT_PUBLIC_UI=v2 npm run build > /tmp/build-v2.log 2>&1; echo "EXIT=$?"; grep -c "v2/" /tmp/build-v2.log; tail -5 /tmp/build-v2.log
git diff 71c60bc..HEAD -- src/lib/actions/rooms.ts "src/app/(staff)/[eventCode]/guests/layout.tsx" "src/app/(staff)/[eventCode]/rsvp/queue/QueueBoard.tsx" 2>&1  ...
git diff 71c60bc..HEAD -- src/lib/errors.ts | head -130
ls supabase/migrations/ 2>/dev/null | tail -20; echo "---"; grep -rn "at max capacity\|overlapping stay\|is before check-in" supabase/migrations/ *.sql 2>/dev/n ...
cat supabase/migrations/20260816150000_room_guard_row_lock.sql
grep -n "raise exception" supabase/migrations/20260816130000_merged_room_guard.sql; echo "=== test messages:"; grep -n "message:\|already booked\|overlapping st ...
grep -n "drop trigger\|create trigger\|drop function\|create or replace function\|^--" supabase/migrations/20260816130000_merged_room_guard.sql | head -40
tail -6 /c/Users/rebel/AppData/Local/Temp/claude/C--dev-EventFlow/b98600fc-dde9-474f-a249-6019f61f2609/tasks/bmbrnnd9x.output 2>/dev/null; echo "---"; grep -n " ...
sed -n '170,205p' DECISIONS.md
V2="src/app/(app)/v2"; echo "=== hardcoded hex in v2:"; grep -rn "#[0-9a-fA-F]\{3,8\}\b" "$V2" --include=*.tsx --include=*.ts | grep -v "^\s*\*" | head -20; ech ...
sed -n '36,85p' src/components/native/OfflineBanner.tsx; echo "=== tests that scan for hex/tokens:"; ls tests/ | grep -i "token\|shadow\|css\|style"
head -40 tests/no-arbitrary-shadows.test.ts; echo "=== SCAN DIRS:"; grep -n "readdir\|join(\|ROOT\|DIRS\|glob" tests/no-arbitrary-shadows.test.ts tests/motion-t ...
grep -E "^EXIT=|^[0-9]+$" /c/Users/rebel/AppData/Local/Temp/claude/C--dev-EventFlow/b98600fc-dde9-474f-a249-6019f61f2609/tasks/bmbrnnd9x.output; grep -c "v2/\[e ...
sed -n '1,140p' "src/app/(app)/v2/[eventCode]/_components/device-flags.ts"
grep -n "useOptimisticAction\|offerUndo\|undo\|submitProof\|DeliveryDetail\|generateDeliverables\|useMutation\|startTransition" "src/app/(app)/v2/[eventCode]/ho ...
echo "=== rsvp_campaigns constraints:"; grep -n "unique\|primary key\|create table" supabase/migrations/20260921130100_rsvp_campaigns.sql | head -20; echo; echo ...
cat src/lib/query/prefetch.ts
git diff 71c60bc..HEAD -- "src/app/(staff)/[eventCode]/guests/list/GuestsClient.tsx" "src/app/(staff)/[eventCode]/logistics/arrivals/ArrivalsClient.tsx" 2>&1 |  ...
sed -n '1,100p' "src/app/(app)/v2/[eventCode]/_components/AppTabs.tsx"
sed -n '1,120p' src/lib/lock.ts
grep -rn "v_rsvp_queue" supabase/migrations/*.sql | grep -i "create\|replace" ; echo "=== latest definition:"; for f in $(grep -rln "create or replace view publ ...
sed -n '55,115p' supabase/migrations/20260812000000_presence_columns.sql
grep -ln "locked_by_staff" supabase/migrations/*.sql; echo "=== in any migration after 20260812000000:"; grep -n "locked_by_staff" supabase/migrations/202608[1- ...
sed -n '60,200p' "src/app/(app)/v2/[eventCode]/hospitality/deliveries/HamperRun.tsx"
grep -n "404\|status\|final\|url()\|pass\|fail\|denied\|pick-staff\|staff" scripts/tabs-reach.mjs | head -45
sed -n '190,215p' "src/app/(app)/v2/[eventCode]/hospitality/deliveries/HamperRun.tsx"; echo "=== GiveRoom empty states:"; sed -n '218,260p' "src/app/(app)/v2/[e ...
sed -n '55,111p' "src/app/(app)/v2/[eventCode]/_components/AppHint.tsx"
grep -n "queryKey\|useQuery\|queryKeys\|findGuests\|readGuests\|staleTime" "src/app/(app)/v2/[eventCode]/find/FindClient.tsx" "src/app/(app)/v2/[eventCode]/find ...
echo "=== tap targets under 44px on interactive elements in v2:"; grep -rn "min-h-[0-9]\+" "src/app/(app)/v2" --include=*.tsx -o | sort | uniq -c | sort -rn; ec ...
sed -n '1,90p' "src/app/(app)/v2/[eventCode]/find/FindClient.tsx"
grep -n "durationOf" -A12 "src/app/(app)/v2/[eventCode]/rsvp/queue/CallNext.tsx" | tail -20; echo "=== submitCallOutcome signature+use of endedAt:"; grep -n "en ...
sed -n '1,60p' src/components/ui/UndoBar.tsx; echo "..."; grep -n "error\|fail" src/components/ui/UndoBar.tsx | head
for f in $(grep -rl "<button" "src/app/(app)/v2" --include=*.tsx); do awk -v F="$f" '/<button/{s=NR; buf=""} s && NR<s+8 {buf=buf" "$0} s && /className=/ && NR> ...
mkdir -p /c/dev/EventFlow-ui2/reviews && cat > /c/dev/EventFlow-ui2/reviews/R1.md <<'EOF' # R1 â€” adversarial review of the "Instant & Obvious" rebuild (71c60b ...
```

## Where it left off

Review written to `C:\dev\EventFlow-ui2\reviews\R1.md`.

**Gates, run here:** `tsc --noEmit` clean Â· `npm run build` green Â· `NEXT_PUBLIC_UI=v2 npm run build` green, 35 v2 routes in the table Â· `vitest run` 275/275.

**2 blockers**

1. **A deferred write that the server refuses after you leave the screen fails silently.** `useOptimisticAction`'s `deferUntilCommit` commit path reports failure via `setLastError` on the component that armed it â€” but the undo store is deliberately module-level so the write survives the unmount. `UndoBar` has no error branch and `SyncChip` only counts *network* failures, so a server refusal (the room guard's 23514 â€” exactly the race GiveRoom's own comment names) rolls the cache back and tells nobody. Hits `GiveRoom` and `MeetArrivals`. Fix: route the deferred failure through the same module store, rendered by `UndoBar`.

2. **Empty-state actions link to sections the viewer's department cannot open.** GiveRoom's "Every family has a room" offers one control, "Go to the call list" â†’ `rsvp/queue`; for a `hospitality` runner `requireSection` bounces them to `hospitality/rooms` â€” the screen they tapped from â€” and nothing renders `?denied=section`. The button appears to do nothing, and that empty state recurs every time a runner clears their queue. HamperRun's "Open the guest list" is the same shape for `hamper` and `hospitality`.

**5 should-fix:** `v_rsvp_queue` no longer carries `locked_by_staff` (verified across all four view definitions and every later migration), so V10's named-holder lock copy is inert and `useStaffNames` is a wasted read on the calling screen Â· a queued call outcome recomputes `ended_at` at replay time into a row that then freezes, contradicting its own `duration_sec` Â· under `v2` the offline banner disappears from `/login`, `/pick-staff` and `/admin/**` Â· `roomGuardCause('overlap')` is dead against the merged guard and DECISIONS.md quotes a message `20260816150000` does not raise (6 tests certify the dead string) Â· `tabs-reach.mjs` scores a redirect as a pass, which is why #2 shipped.

**Could not verify:** anything needing a handset, a live server or the database â€” specifically whether `20260816150000` is actually applied to the live project, and the `locked_by_staff` absence against the live view rather than the migration files (one `select` settles it). House rules were swept statically, not rendered at 360px.

---

See also: [[Status]], [[Roadmap]], [[Known Traps]], [[Schema Reality Check]]
