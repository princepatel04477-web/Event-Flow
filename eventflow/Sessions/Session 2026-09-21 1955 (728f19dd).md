---
tags: [session-log, auto-captured]
session: 728f19dd-ca04-4150-8089-9f084af61f42
captured_by: SessionEnd
started: 2026-09-21 19:55
updated: 2026-09-21 19:59
---

# Session 2026-09-21 1955 (728f19dd)

Auto-captured from the Claude Code transcript on `SessionEnd`.
Curated project knowledge lives in [[EventFlow]]. This note is raw history.

## What was asked

- ï»¿You are performing an adversarial, READ-ONLY verification of work just completed in C:\dev\EventFlow by a different AI agent (Antigravity/Gemini). Do NOT edit, create or delete any file. Report findings only. CONTEXT: this app was a dark-teal night palette (#071A1D + champagne brass) before a "Royal Ivory & Gold" re-skin. The colour tokens re-skinned; shadows, inline styles and code comments di ...

## Commands run (22 total, 22 distinct)

```
git status --porcelain && echo "=== DIFFSTAT ===" && git diff --stat
grep -n "ef-shadow\|shadow-e1\|shadow-e2\|shadow-e3" src/app/globals.css
sed -n '130,215p' src/app/globals.css
git diff android/app/build.gradle
node -e " const lin=c=>{c/=255;return c<=0.04045?c/12.92:Math.pow((c+0.055)/1.055,2.4)}; const L=h=>{const n=parseInt(h.slice(1),16);const r=(n>>16)&255,g=(n>>8 ...
echo "=== shadow-[ in src tsx/ts ===" && grep -rn 'shadow-\[' src/ --include=*.tsx --include=*.ts ; echo "--- exit $? ---"; echo "=== shadow-[ anywhere in src = ...
cat -n src/app/global-error.tsx
cat -n tests/no-arbitrary-shadows.test.ts
git diff src/app/global-error.tsx
git diff src/app/globals.css
grep -n "ef-paper:\|ef-paper-band:\|ef-surface:" src/app/globals.css | head -20
git diff 'src/app/(auth)/layout.tsx' src/components/ui/ 'src/app/(staff)/[eventCode]/guests/_components/GuestCard.tsx' 'src/app/(staff)/[eventCode]/guests/list/ ...
sed -n '50,100p' src/app/globals.css
echo "=== stale prose grep ===" && grep -rniE 'night|teal|071A1D|C9A96B|verdigris|brass|dark ground|#EDF3F2|8FA9AE' src/ || echo NONE
node -e " const lin=c=>{c/=255;return c<=0.04045?c/12.92:Math.pow((c+0.055)/1.055,2.4)}; const L=h=>{const n=parseInt(h.slice(1),16);return 0.2126*lin((n>>16)&2 ...
echo "=== tailwind config? ===" && ls tailwind.config.* 2>/dev/null || echo "none (good)"; echo "=== package.json changed? ==="; git diff --stat package.json pa ...
grep -n "ef-brand-tint\|ef-brand-fg\|ef-brand:" src/app/globals.css | head; echo "=== DeliveryDetail seal context ==="; sed -n '392,425p' 'src/app/(staff)/[even ...
sed -n '78,100p' src/components/ui/Field.tsx; echo "=== DECISIONS.md appended block ==="; git diff DECISIONS.md | head -60
sed -n '26,40p' src/app/globals.css; echo "--- 230..240 ---"; sed -n '230,240p' src/app/globals.css
echo "=== other arbitrary shadow forms ==="; grep -rnE 'drop-shadow-\[|\[box-shadow|shadow-brand|boxShadow' src/ || echo NONE; echo "=== className in .ts (non-t ...
sed -n '25,45p' src/components/ui/Card.tsx; echo "=== run the new test ==="; npx vitest run tests/no-arbitrary-shadows.test.ts 2>&1 | tail -15
stat -c '%y %n' android/app/build.gradle src/app/globals.css src/app/global-error.tsx tests/no-arbitrary-shadows.test.ts DECISIONS.md src/components/ui/Card.tsx
```

## Where it left off

## Verification report â€” read-only, nothing modified

### Defects

**1. `src/app/global-error.tsx:24` â€” the button contrast ratio is wrong, and the AAA label is false.**
The comment says `#ffffff on #735c00 is 7.4:1 (AAA)`. Computed value is **6.44:1**. AAA for normal text requires â‰¥7.0, so the parenthetical is incorrect â€” this pairing is AA, not AAA. In a comment that opens `Measured WCAG contrast ratios:`, that is the worst kind of wrong: it reads as verified and will be trusted by the next person deciding whether the button is compliant.

**2. `src/app/global-error.tsx:22` â€” the body-text ratio is overstated.**
`#191c1d on #f8f9fa is 16.8:1` â€” computed **16.26:1**. Still comfortably AAA, so this is cosmetic, but it is inaccurate under a "Measured" heading.

*Root cause note, not a defect of this session:* both wrong numbers were copied from pre-existing comments in `globals.css`, which are themselves wrong and were not touched â€” `globals.css:67` claims `17.6:1 on white` (actual **17.14**) and `globals.css:96` claims `#735c00 â€¦ reads 7.4:1 as text on ivory` (actual **6.11:1**, which is below the 7.0 AAA line the surrounding prose leans on). The agent propagated an existing error rather than inventing one, but it also did not catch it while writing a block that asserts measurement.

**3. `src/components/ui/Card.tsx:37` and `src/app/(staff)/[eventCode]/page.tsx:198` â€” the same shadow was split into two different tiers.**
Both sites carried the identical source value `shadow-[0_16px_40px_-22px_rgba(0,0,0,0.85)]`. Card's `hero` tone became `shadow-e1`; the event-home headline panel became `shadow-e2`. These are the same role â€” the one lifted figure on a screen â€” and the token comment at `globals.css:149` explicitly assigns e2 to "the headline panel". Two hero surfaces now render at visibly different elevations. `DECISIONS.md` records the split without explaining it, so this reads as an oversight rather than a decision.

**4. `src/app/(staff)/[eventCode]/hospitality/deliveries/[deliverableId]/DeliveryDetail.tsx:406` â€” the edit exceeded the stated scope and left the seal visually incoherent.**
The claim was "46px gold bloom â†’ emerald tint". The diff also swapped the radial **fill** from `var(--ef-brand-tint)` to `var(--ef-green-tint)`. That is an unrequested colour change to a rendered surface, not a shadow fix. The result is a seal whose ring (`border-2 border-brand`), inner ring (`border-brand/45`), "SEALED" lettering (`text-brand`) and divider (`bg-brand/50`) are all gold, sitting on a green interior wash. Nothing else in the file moved to emerald. Either the gold elements should follow, or the fill should have stayed `--ef-brand-tint`.

**5. Stale "brass" prose survives in four places the session did not sweep, and its own rewrites made the inconsistency worse.**
The session replaced *brass* with *gold* in `Button.tsx`, `Card.tsx` and `Field.tsx`. These still name the `#735c00` gold accent as brass:
- `src/app/globals.css:233` â€” `/* Brass â€” the accent. */`, labelling the `--color-brand` block
- `src/components/ui/Chip.tsx:12` â€” "Defaults to brass"
- `src/components/ui/StatusPill.tsx:19` â€” "`active` â€” brass. In hand, in progress"
- `src/app/(staff)/[eventCode]/logistics/fleet/FleetClient.tsx:184` â€” "mono, brass, letter-spaced"

These are outside your sanctioned-survivor list (which covers `globals.css:87`, `:188/194`, `layout.tsx:121`, and *verdigris* in StatusPill/ReviewPanel). `globals.css:233` is the one that matters most â€” it sits directly above the token it mislabels.

**6. `android/app/build.gradle` is modified and is outside the allowed file list.**
Flagging per instruction. Content is `versionCode 6 â†’ 7` / `final-06 â†’ final-07` with a comment about the navigation rebuild â€” unrelated to the re-skin. `mtime` is `15:50`, roughly four hours ahead of this session's edit window (`19:47`â€“`19:53`), so it is uncommitted carry-over from the prior nav session, not this agent's work. 

...truncated...

---

See also: [[Status]], [[Roadmap]], [[Known Traps]], [[Schema Reality Check]]
