# Latency — the measurement

<!-- GENERATED by `npm run latency` (scripts/latency.mjs). Do not hand-edit: the next run overwrites it. -->

Budgets are `.brain/SPEC-PERF.md` §Measurement: tap → first visual change **≤120 ms**, tap → content on a warm store **≤120 ms**, save → shown done **≤120 ms**, cold first load **≤2500 ms on 4g** (informational). The gate is the **p50**; p95 is published beside it.

| | |
|---|---|
| Measured | 2026-09-23T18:47:35.543Z |
| Base URL | `http://localhost:3000` |
| Event | `NOT MEASURED — see .brain/report-perfmeasure.md` |
| Runs per metric | 5 |
| Profiles | 4g, venue-wifi |
| Viewport / CPU | 390×844, 4× CPU throttle |

## Results

| Screen | Profile | Metric | p50 | p95 | Budget | n | Verdict |
|---|---|---|---:|---:|---:|---:|---|
| Today | 4g | Cold first load | — | — | 2.50 s | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Today | 4g | Tap → first visual change | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Today | 4g | Tap → content (warm) | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Today | 4g | Save → shown done | — | — | 120 ms | 0 | not measured — Today is a read-only dashboard: it has no write to time. |
| Calls | 4g | Cold first load | — | — | 2.50 s | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Calls | 4g | Tap → first visual change | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Calls | 4g | Tap → content (warm) | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Calls | 4g | Save → shown done | — | — | 120 ms | 0 | not measured — No reversible write: logging a call outcome freezes its call_attempts row permanently (CLAUDE.md §5.4), so timing a "save" here would write data the TEST event cannot take back. |
| Rooms | 4g | Cold first load | — | — | 2.50 s | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Rooms | 4g | Tap → first visual change | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Rooms | 4g | Tap → content (warm) | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Rooms | 4g | Save → shown done | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Check-in | 4g | Cold first load | — | — | 2.50 s | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Check-in | 4g | Tap → first visual change | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Check-in | 4g | Tap → content (warm) | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Check-in | 4g | Save → shown done | — | — | 120 ms | 0 | not measured — No reversible write on this screen: check-in writes are not offered an Undo, and the harness must not leave a real arrival recorded on the TEST event. |
| Hampers | 4g | Cold first load | — | — | 2.50 s | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Hampers | 4g | Tap → first visual change | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Hampers | 4g | Tap → content (warm) | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Hampers | 4g | Save → shown done | — | — | 120 ms | 0 | not measured — No reversible write: a hamper proof is insert-only and cannot be deleted by any role, so there is nothing to undo after timing it. |
| Arrivals | 4g | Cold first load | — | — | 2.50 s | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Arrivals | 4g | Tap → first visual change | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Arrivals | 4g | Tap → content (warm) | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Arrivals | 4g | Save → shown done | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Guests search | 4g | Cold first load | — | — | 2.50 s | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Guests search | 4g | Tap → first visual change | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Guests search | 4g | Tap → content (warm) | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Guests search | 4g | Save → shown done | — | — | 120 ms | 0 | not measured — Search is a read: typing produces results, and no write happens on this screen. |
| Today | venue-wifi | Cold first load | — | — | 2.50 s | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Today | venue-wifi | Tap → first visual change | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Today | venue-wifi | Tap → content (warm) | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Today | venue-wifi | Save → shown done | — | — | 120 ms | 0 | not measured — Today is a read-only dashboard: it has no write to time. |
| Calls | venue-wifi | Cold first load | — | — | 2.50 s | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Calls | venue-wifi | Tap → first visual change | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Calls | venue-wifi | Tap → content (warm) | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Calls | venue-wifi | Save → shown done | — | — | 120 ms | 0 | not measured — No reversible write: logging a call outcome freezes its call_attempts row permanently (CLAUDE.md §5.4), so timing a "save" here would write data the TEST event cannot take back. |
| Rooms | venue-wifi | Cold first load | — | — | 2.50 s | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Rooms | venue-wifi | Tap → first visual change | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Rooms | venue-wifi | Tap → content (warm) | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Rooms | venue-wifi | Save → shown done | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Check-in | venue-wifi | Cold first load | — | — | 2.50 s | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Check-in | venue-wifi | Tap → first visual change | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Check-in | venue-wifi | Tap → content (warm) | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Check-in | venue-wifi | Save → shown done | — | — | 120 ms | 0 | not measured — No reversible write on this screen: check-in writes are not offered an Undo, and the harness must not leave a real arrival recorded on the TEST event. |
| Hampers | venue-wifi | Cold first load | — | — | 2.50 s | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Hampers | venue-wifi | Tap → first visual change | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Hampers | venue-wifi | Tap → content (warm) | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Hampers | venue-wifi | Save → shown done | — | — | 120 ms | 0 | not measured — No reversible write: a hamper proof is insert-only and cannot be deleted by any role, so there is nothing to undo after timing it. |
| Arrivals | venue-wifi | Cold first load | — | — | 2.50 s | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Arrivals | venue-wifi | Tap → first visual change | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Arrivals | venue-wifi | Tap → content (warm) | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Arrivals | venue-wifi | Save → shown done | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Guests search | venue-wifi | Cold first load | — | — | 2.50 s | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Guests search | venue-wifi | Tap → first visual change | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Guests search | venue-wifi | Tap → content (warm) | — | — | 120 ms | 0 | not measured — no browser on the host that wrote this file — run `npm run latency` where one can start. |
| Guests search | venue-wifi | Save → shown done | — | — | 120 ms | 0 | not measured — Search is a read: typing produces results, and no write happens on this screen. |

## Verdict

**Nothing was measured.** Every row above is `not measured`, for the reason in its last column. This file proves the harness ran; it says nothing about how fast the app is.

